# --- engine core (needed to run locally) ---
langgraph
pyyaml

# --- real providers: install only the ones you use ---
anthropic                    # WORKER_PROVIDER=anthropic
snowflake-snowpark-python    # WORKER_PROVIDER=cortex  / SQL_TOOL=cortex
openai                       # WORKER_PROVIDER=databricks (OpenAI-compatible endpoint)
databricks-sdk               # SQL_TOOL=genie

# --- only needed to DEPLOY on Snowflake (SPCS) ---
fastapi
uvicorn

# --- only needed to DEPLOY on databricks ---
mlflow
databricks-agents
