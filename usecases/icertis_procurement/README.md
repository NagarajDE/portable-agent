# icertis_procurement — active-contract analytics

Mirrors the Analyst side of native `DBADMIN.AGENTS.ICERTIS_PROCUREMENT_AGENT` (read-only reference).
That agent also has a Cortex Search tool over contract documents; this pack mirrors the structured
analytics (Cortex Analyst) side.

- Semantic view (live): `OPERATIONS_DEV.OPERATIONS_SANDBOX.ICERTIS_PROCUREMENT_ANALYTICS` (declared in `semantic_layer.yaml`)
- House rules distilled into `skills/contract_analytics.md` and `prompts/rubric.md`.
