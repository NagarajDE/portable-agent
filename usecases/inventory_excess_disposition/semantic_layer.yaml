# Native semantic layer for this Analyst-backed pack. PRESENCE of this file marks the pack AI+BI;
# engine/ reads the binding from HERE, never from env. Same native view as the inventory_balance pack.
snowflake:
  view: OPERATIONS_DEV.OPERATIONS_SANDBOX.INVENTORY_ANALYTICS   # a native Semantic View
