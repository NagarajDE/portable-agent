# Native semantic layer for this AI+BI/Analyst pack. The PRESENCE of this file marks the pack as
# Analyst-backed; engine/ reads the binding from HERE, never from env (any env-vs-pack override is
# a test-only concern in the runner scripts). Mirrors native agent PROCUREMENT_CONTRACT_AGENT.
snowflake:
  view: DB_ENTERPRISE_DW_VAL.GSC_PROCUREMENT.ICERTIS_CONTRACTS   # a native Semantic View
